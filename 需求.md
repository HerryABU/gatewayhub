好的，我来为你撰写一份**完整版、可直接用于项目立项和开发的需求文档**。这份文档将涵盖所有功能细节、技术方案、数据库设计、接口规范、界面原型和开发排期。

---

# 多应用聚合网关（统一接入与控制台）
## 需求规格说明书（完整版）

**文档版本**：V1.0  
**拟制日期**：2026-08-17  
**文档状态**：定稿  
**密级**：内部公开

---

## 目录

1. [项目概述](#1-项目概述)
2. [术语定义](#2-术语定义)
3. [用户角色与权限](#3-用户角色与权限)
4. [功能需求](#4-功能需求)
5. [非功能需求](#5-非功能需求)
6. [数据库设计](#6-数据库设计)
7. [接口设计](#7-接口设计)
8. [界面原型](#8-界面原型)
9. [技术方案](#9-技术方案)
10. [部署方案](#10-部署方案)
11. [项目排期](#11-项目排期)
12. [附录](#12-附录)

---

## 1. 项目概述

### 1.1 项目名称
多应用聚合网关（统一接入与控制台）

### 1.2 项目代号
**GatewayHub**

### 1.3 项目背景

公司内部已建设多个业务系统，技术栈涵盖 Java、Go、Node.js、Python 等。各系统独立部署，暴露不同的 IP 地址和端口号，导致以下问题：

| 问题 | 具体表现 |
| :--- | :--- |
| **入口分散** | 开发/测试/运维人员需记忆十余个不同端口号，如 `:8080`、`:8081`、`:3000`、`:5000` |
| **变更繁琐** | 新增或迁移服务时，需协调运维修改 Nginx 配置并重载，流程耗时 1-3 天 |
| **缺乏统一视图** | 无法在一个页面查看所有服务是否在线，排障需逐个检查 |
| **无访问统计** | 无法知道哪些服务被调用了多少次，难以评估服务使用热度 |

### 1.4 项目目标

构建一个 **轻量级、带管理界面、支持动态配置** 的统一接入网关，实现：

1. **统一入口**：所有服务通过 **一个端口** 对外暴露，路径格式为 `/{自定义名称}/...`
2. **动态路由**：通过 Web 管理界面 **实时增删改** 路由规则，**无需重启**
3. **服务可观测**：实时展示每个服务的健康状态
4. **访问统计**：记录并展示每个路由的 PV 数据（总、今日、趋势）
5. **分级权限**：访客只读，管理员可管理

### 1.5 项目范围

| 包含 | 不包含 |
| :--- | :--- |
| HTTP/1.1 反向代理转发 | HTTPS 原生支持（由前置 Nginx 卸载） |
| 基于路径前缀的路由分发 | WebSocket 代理转发（V2.0） |
| Web 管理控制台（Vue 3） | 多节点负载均衡（V1.1） |
| 路由 CRUD（增删改查） | 限流/熔断（V2.0） |
| 健康检查与状态展示 | API 网关鉴权（V2.0） |
| 访问量统计（PV） | 灰度发布（V2.0） |
| 访客/管理员双角色 | 服务注册发现（V2.0） |

---

## 2. 术语定义

| 术语 | 定义 |
| :--- | :--- |
| **转发名** | 管理员自定义的对外路径名称，如 `java-order`，访问时路径为 `/{转发名}/...` |
| **后端地址** | 目标服务的实际地址，支持 `:8080` 或 `:8080/api/v1` 两种格式 |
| **访客** | 未登录用户，仅可查看服务列表和状态 |
| **管理员** | 登录用户，拥有路由管理和统计查看权限 |
| **PV** | Page View，页面访问量，本项目中指每个路由收到的请求总数 |
| **健康检查** | 网关主动探测后端服务是否存活，通过调用 `/health` 端点判断 |

---

## 3. 用户角色与权限

### 3.1 角色定义

| 角色 | 标识 | 访问方式 | 说明 |
| :--- | :--- | :--- | :--- |
| **访客** | `visitor` | 无需登录，直接访问首页 | 默认角色，所有用户进入时的初始状态 |
| **管理员** | `admin` | 需通过登录页输入账号密码 | 拥有全部管理权限 |

### 3.2 权限矩阵

| 功能模块 | 访客（无需登录） | 管理员（登录后） |
| :--- | :---: | :---: |
| **服务列表查看** | ✅ 只读 | ✅ 只读 |
| **服务健康状态查看** | ✅ | ✅ |
| **路由新增** | ❌ | ✅ |
| **路由编辑** | ❌ | ✅ |
| **路由删除** | ❌ | ✅ |
| **路由启用/停用** | ❌ | ✅ |
| **统计概览查看** | ❌ | ✅ |
| **路由 PV 明细查看** | ❌ | ✅ |
| **趋势图查看** | ❌ | ✅ |
| **管理员登录** | ✅（登录入口） | N/A |

### 3.3 访客与普通用户的说明

> **注**：本系统仅设两种角色——**访客**和**管理员**。文档中提及的"普通用户"即指**访客**角色。

---

## 4. 功能需求

### 4.1 模块一：核心转发引擎

#### 4.1.1 统一监听

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| 监听地址 | `0.0.0.0` | 支持绑定指定 IP |
| 监听端口 | `8088` | 可通过配置文件修改 |
| 最大请求头大小 | `1MB` | 防止大请求攻击 |
| 读取超时 | `30s` | 读取请求超时 |
| 写入超时 | `30s` | 响应写入超时 |

#### 4.1.2 路由匹配规则

**匹配流程**：

1. 从请求 URL 中提取第一段路径作为 `转发名`
   - 例如：`/java-order/user/info` → 转发名为 `java-order`
2. 在内存路由表中查找匹配项
3. 若找到，执行转发；若未找到，返回 `404 Not Found`

**转发名约束**：
- 仅允许小写字母、数字、连字符（`-`）
- 长度限制：3-30 个字符
- 必须以字母开头
- 全局唯一

#### 4.1.3 路径转发逻辑

| 后端配置格式 | 示例 | 客户端请求 | 实际转发至 |
| :--- | :--- | :--- | :--- |
| `:{port}` | `:8080` | `/java-order/user/info` | `http://127.0.0.1:8080/user/info` |
| `:{port}/{root}` | `:8080/api/v1` | `/java-order/user/info` | `http://127.0.0.1:8080/api/v1/user/info` |
| `http://{host}:{port}` | `http://192.168.1.10:8080` | `/java-order/user/info` | `http://192.168.1.10:8080/user/info` |
| `http://{host}:{port}/{root}` | `http://192.168.1.10:8080/api` | `/java-order/user/info` | `http://192.168.1.10:8080/api/user/info` |
| `https://{host}:{port}` | `https://backend.internal:8443` | `/java-order/user/info` | `https://backend.internal:8443/user/info` |

**路径拼接规则**：
- 若后端配置包含根路径（如 `/api/v1`），将根路径与请求路径**直接拼接**
- 若请求路径为 `/java-order/`，转发时变为 `/api/v1/`（保留斜杠）

#### 4.1.4 请求头处理

| 操作 | 说明 |
| :--- | :--- |
| 透传所有原始请求头 | 保留 `Cookie`、`Authorization` 等 |
| 添加 `X-Forwarded-For` | 记录客户端真实 IP 链 |
| 添加 `X-Real-IP` | 记录客户端真实 IP |
| 添加 `X-Forwarded-Proto` | 记录原始协议（http/https） |
| 修改 `Host` 头 | 设置为后端目标 Host |

#### 4.1.5 超时与错误处理

| 场景 | 处理方式 | 返回状态码 |
| :--- | :--- | :--- |
| 后端连接超时（默认 5s） | 中断请求，返回错误 | `504 Gateway Timeout` |
| 后端主动拒绝连接 | 记录日志，返回错误 | `502 Bad Gateway` |
| 后端返回非 2xx 状态码 | 原样透传给客户端 | 透传后端状态码 |
| 路由不存在 | 返回 JSON 错误信息 | `404 Not Found` |
| 路由处于停用状态 | 返回 JSON 错误信息 | `503 Service Unavailable` |

### 4.2 模块二：健康检查

#### 4.2.1 检查方式

**HTTP 端点探测**（默认）：
- 调用后端服务的 `/health` 端点
- 超时时间：2 秒
- 判断标准：HTTP 状态码为 `200`

**TCP 端口探测**（备选）：
- 当后端无 `/health` 端点时，尝试 TCP 连接
- 超时时间：2 秒
- 判断标准：连接成功

#### 4.2.2 检查周期

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| 检查间隔 | `30s` | 每个路由独立探测 |
| 失败阈值 | `3` | 连续失败 3 次标记为不健康 |
| 恢复阈值 | `2` | 连续成功 2 次标记为健康 |

#### 4.2.3 状态展示

| 状态 | 图标 | 说明 |
| :--- | :--- | :--- |
| 健康 | 🟢 绿色 | 后端服务正常响应 |
| 不健康 | 🔴 红色 | 后端服务无响应或异常 |
| 未知 | ⚪ 灰色 | 尚未探测或探测配置缺失 |

### 4.3 模块三：管理控制台

#### 4.3.1 访客页面（`/`）

**页面元素**：

| 区域 | 内容 |
| :--- | :--- |
| **顶部导航** | 系统 Logo + 标题 "GatewayHub 服务看板" + "管理员登录" 按钮 |
| **服务列表** | 表格展示所有路由，包含列：服务名称、转发名、后端地址、健康状态 |
| **统计摘要** | 服务总数、健康数、不健康数（显示在表格上方） |
| **自动刷新** | 每 10 秒自动刷新健康状态 |

**访问控制**：
- 无需任何认证，直接访问首页即可查看

#### 4.3.2 管理员登录页

**登录入口**：
- 点击右上角 "管理员登录" 按钮，弹出登录对话框（Modal）

**登录表单**：
| 字段 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| 用户名 | 文本输入 | ✅ | 默认管理员：`admin` |
| 密码 | 密码输入 | ✅ | 默认密码：`admin123` |
| 记住我 | 复选框 | ❌ | 延长 Token 有效期至 7 天 |

**登录验证**：
- 前端校验：非空校验
- 后端校验：用户名存在 + 密码匹配（bcrypt）
- 成功：返回 JWT Token，存入 localStorage
- 失败：提示 "用户名或密码错误"

#### 4.3.3 管理员后台

**访问路径**：登录后自动跳转至 `/dashboard`

**顶部导航变化**：
- 原 "管理员登录" 变为头像 + 用户名 + 下拉菜单（"退出登录"）
- 新增 Tab 切换：`📋 路由管理` | `📊 统计面板`

---

#### 4.3.4 路由管理页面

**列表展示**：

| 列名 | 说明 |
| :--- | :--- |
| 序号 | 自增编号 |
| 服务名称 | 管理员自定义的服务展示名 |
| 转发名 | 对外的路径名称（如 `java-order`） |
| 后端地址 | 配置的目标地址（如 `:8080` 或 `:8080/api/v1`） |
| 超时 | 当前配置的超时时间（秒） |
| 状态 | 启用/停用 切换开关 |
| 健康状态 | 🟢/🔴 实时状态 |
| 操作 | 编辑 / 删除 按钮 |

**新增路由（弹窗/抽屉）**：

| 字段 | 类型 | 必填 | 校验规则 | 说明 |
| :--- | :--- | :--- | :--- | :--- |
| 服务名称 | 文本输入 | ✅ | 2-30 字符，不可为空 | 展示用，如 "Java-订单服务" |
| 转发名 | 文本输入 | ✅ | 3-30 字符，字母开头，仅小写/数字/-，全局唯一 | 对外路径名，如 `java-order` |
| 后端地址 | 文本输入 | ✅ | 格式校验：`:端口` 或 `:端口/路径` 或完整 URL | 支持 `:8080`、`:8080/api/v1` |
| 超时时间 | 数字输入 | ❌ | 1-60 秒，默认 5 | 后端响应超时 |
| 状态 | 开关 | ❌ | 默认启用 | 启用/停用 |

**编辑路由**：
- 所有字段均可修改（转发名除外）
- 转发名不可修改，如需变更，先删除再新增

**删除路由**：
- 二次确认弹窗："确认删除路由 [服务名称]？此操作不可恢复"
- 删除后即时生效

#### 4.3.5 统计面板页面

**顶部统计卡片**（4 个）：

| 卡片 | 数据来源 | 说明 |
| :--- | :--- | :--- |
| 总 PV | `access_logs` 全表计数 | 历史累计所有请求总数 |
| 今日 PV | `access_logs` 当日计数 | 今日 00:00 至现在的请求数 |
| 路由总数 | `routes` 表计数 | 已配置的路由数量 |
| 健康率 | 健康数/总数 × 100% | 展示为百分比，如 "83%" |

**趋势图**（ECharts 折线图）：

| 配置项 | 说明 |
| :--- | :--- |
| 时间范围 | 近 7 天（默认）/ 近 30 天（切换） |
| 数据粒度 | 按天聚合 |
| Y 轴 | 请求数量 |
| X 轴 | 日期（如 08-10、08-11） |
| 交互 | 鼠标悬停显示具体数值 |

**路由 PV 明细表格**：

| 列名 | 说明 |
| :--- | :--- |
| 排名 | 按总 PV 降序排列 |
| 转发名 | 如 `/java-order`（带斜杠展示） |
| 服务名称 | 如 "Java-订单服务" |
| 总 PV | 历史累计请求数 |
| 今日 PV | 当日请求数 |
| 昨日 PV | 昨日全天请求数 |
| 近 7 日趋势 | 微型折线图（Sparkline），直观展示近期变化 |

**路由详情弹窗**（点击行触发）：
- 该路由近 7 日趋势图（大图）
- 该路由近 7 日逐日数据表格

### 4.4 模块四：访问统计

#### 4.4.1 统计时机

每个请求在 **转发完成后** 异步记录日志，记录内容：

| 字段 | 说明 |
| :--- | :--- |
| `route_prefix` | 转发名（如 `java-order`） |
| `request_path` | 完整请求路径（如 `/java-order/user/info`） |
| `method` | HTTP 方法（GET/POST/PUT/DELETE） |
| `status_code` | 响应状态码（如 200、404、500） |
| `client_ip` | 客户端真实 IP（从 X-Forwarded-For 获取） |
| `user_agent` | 客户端 User-Agent |
| `response_time` | 响应耗时（毫秒） |
| `created_at` | 请求时间 |

#### 4.4.2 写入策略

| 策略 | 说明 |
| :--- | :--- |
| **异步写入** | 日志写入采用 Go Channel + Worker Pool，不阻塞主转发流程 |
| **批量插入** | 每 100 条或每 1 秒批量写入数据库 |
| **失败降级** | 数据库写入失败时，日志暂存内存缓冲区（最多 10000 条），恢复后补写 |
| **采样写入** | 当 QPS > 10000 时，按 10% 采样记录（可配置） |

#### 4.4.3 数据清理

| 配置项 | 默认值 | 说明 |
| :--- | :--- | :--- |
| 保留天数 | `90` 天 | 自动清理 90 天前的日志 |
| 清理周期 | 每天凌晨 03:00 | 定时任务执行 |
| 手动清理 | 支持管理员在后台触发 | 清理全部或指定时间范围 |

#### 4.4.4 性能预估

| 场景 | QPS | 日日志量 | 月日志量 | 90 天日志量 |
| :--- | :--- | :--- | :--- | :--- |
| 小型项目 | 100 | ~86 万条 | ~2580 万条 | ~7740 万条 |
| 中型项目 | 1000 | ~864 万条 | ~2.6 亿条 | ~7.7 亿条 |
| 大型项目 | 10000（采样） | ~864 万条（10%） | ~2.6 亿条 | ~7.7 亿条 |

> **建议**：当单表数据量超过 1 亿条时，考虑按月份分表或迁移至 ClickHouse。

---

## 5. 非功能需求

### 5.1 性能需求

| 指标 | 要求 | 测试条件 |
| :--- | :--- | :--- |
| **转发 QPS** | ≥ 5000 | 单节点，4C8G，后端响应 10ms |
| **转发延迟增加** | < 5ms（P99） | 转发延迟 = 网关处理耗时 - 网络耗时 |
| **内存占用** | < 200MB（空闲）< 500MB（满负载） | 1000 路由配置，10k QPS |
| **CPU 占用** | < 50%（满负载） | 4C 实例，10k QPS |
| **数据库写入** | 异步写入，不阻塞主流程 | 批量写入间隔 < 100ms |

### 5.2 可用性需求

| 指标 | 要求 |
| :--- | :--- |
| **服务可用性** | ≥ 99.9%（月故障时间 < 43 分钟） |
| **优雅重启** | 进程重启时，现有连接不中断（Graceful Shutdown） |
| **故障恢复** | 进程崩溃后自动重启（Systemd 或 Supervisor） |
| **配置热加载** | 路由变更毫秒级生效，无需重启 |

### 5.3 安全性需求

| 需求 | 说明 |
| :--- | :--- |
| **密码加密** | 使用 bcrypt（cost=10）存储 |
| **JWT Token** | HS256 签名，有效期 24 小时（可配置） |
| **防暴力破解** | 登录失败 5 次锁定账号 15 分钟（可配置） |
| **SQL 注入防护** | 使用 GORM ORM，参数化查询 |
| **XSS 防护** | Vue 自动转义，后端 API 返回 JSON |

### 5.4 可扩展性需求

| 需求 | 说明 |
| :--- | :--- |
| **数据库切换** | 支持 SQLite / MySQL / PostgreSQL 无缝切换 |
| **多节点部署** | 支持水平扩展（共享数据库 + 独立内存路由表） |
| **插件机制** | 预留中间件接口，便于后续添加限流、鉴权 |

### 5.5 兼容性需求

| 客户端 | 支持版本 |
| :--- | :--- |
| 浏览器 | Chrome 90+ / Firefox 88+ / Edge 90+ / Safari 14+ |
| HTTP 客户端 | 任何支持 HTTP/1.1 的客户端（curl、Postman、各语言 SDK） |

---

## 6. 数据库设计

### 6.1 ER 图

```
┌─────────────┐       ┌─────────────┐
│    users    │       │   routes    │
├─────────────┤       ├─────────────┤
│ id (PK)     │       │ id (PK)     │
│ username    │       │ name        │
│ password    │       │ prefix (UK) │
│ role        │       │ target      │
│ created_at  │       │ timeout     │
│ updated_at  │       │ status      │
└─────────────┘       │ created_at  │
                      │ updated_at  │
                      └─────────────┘
                            │
                            │ 1 : N
                            ▼
┌─────────────────────────────────┐
│         access_logs             │
├─────────────────────────────────┤
│ id (PK)                         │
│ route_prefix (FK → routes)      │
│ request_path                    │
│ method                          │
│ status_code                     │
│ client_ip                       │
│ user_agent                      │
│ response_time (ms)              │
│ created_at (INDEX)              │
└─────────────────────────────────┘
```

### 6.2 表结构详述

#### 6.2.1 users（用户表）

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | INTEGER | PRIMARY KEY AUTOINCREMENT | 自增主键 |
| `username` | VARCHAR(50) | NOT NULL UNIQUE | 用户名，用于登录 |
| `password` | VARCHAR(255) | NOT NULL | bcrypt 加密密码 |
| `role` | VARCHAR(20) | NOT NULL DEFAULT 'admin' | 角色：admin |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**初始数据**：
```sql
INSERT INTO users (username, password, role) 
VALUES ('admin', '$2a$10$...bcrypt-hash...', 'admin');
```
默认密码：`admin123`

#### 6.2.2 routes（路由配置表）

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | INTEGER | PRIMARY KEY AUTOINCREMENT | 自增主键 |
| `name` | VARCHAR(100) | NOT NULL | 服务展示名称，如 "Java-订单服务" |
| `prefix` | VARCHAR(30) | NOT NULL UNIQUE | 转发名，如 `java-order` |
| `target` | VARCHAR(500) | NOT NULL | 后端地址，如 `:8080` 或 `:8080/api/v1` |
| `timeout` | INTEGER | NOT NULL DEFAULT 5 | 超时时间（秒），范围 1-60 |
| `status` | VARCHAR(20) | NOT NULL DEFAULT 'active' | 状态：active / inactive |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：`idx_routes_prefix` 在 `prefix` 字段

#### 6.2.3 access_logs（访问日志表）

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | BIGINT | PRIMARY KEY AUTOINCREMENT | 自增主键（支持大数据量） |
| `route_prefix` | VARCHAR(30) | NOT NULL | 转发名，与 routes.prefix 关联 |
| `request_path` | VARCHAR(500) | NULL | 完整请求路径 |
| `method` | VARCHAR(10) | NULL | HTTP 方法 |
| `status_code` | SMALLINT | NULL | 响应状态码 |
| `client_ip` | VARCHAR(45) | NULL | 客户端 IP（支持 IPv6） |
| `user_agent` | VARCHAR(500) | NULL | User-Agent 字符串 |
| `response_time` | INTEGER | NULL | 响应耗时（毫秒） |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 请求时间 |

**索引**：
- `idx_logs_route_created` 在 `(route_prefix, created_at)` 复合索引
- `idx_logs_created` 在 `created_at`（用于清理）

#### 6.2.4 daily_stats（日统计汇总表）- 可选，用于性能优化

| 字段名 | 类型 | 约束 | 说明 |
| :--- | :--- | :--- | :--- |
| `id` | INTEGER | PRIMARY KEY AUTOINCREMENT | 自增主键 |
| `route_prefix` | VARCHAR(30) | NOT NULL | 转发名 |
| `stat_date` | DATE | NOT NULL | 统计日期 |
| `pv` | BIGINT | NOT NULL DEFAULT 0 | 当日 PV |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP | 更新时间 |

**索引**：`idx_daily_route_date` 在 `(route_prefix, stat_date)` 复合唯一索引

---

## 7. 接口设计

### 7.1 通用规范

**请求格式**：`application/json`

**响应格式**：
```json
{
    "code": 0,          // 0=成功，非0=错误码
    "message": "success",
    "data": { ... }     // 具体数据
}
```

**错误码定义**：

| 错误码 | 说明 |
| :--- | :--- |
| 0 | 成功 |
| 1001 | 参数校验失败 |
| 1002 | 资源不存在 |
| 1003 | 资源已存在（冲突） |
| 1004 | 权限不足 |
| 1005 | 未认证（Token 无效/过期） |
| 2001 | 数据库错误 |
| 2002 | 后端服务不可达 |
| 2003 | 操作超时 |

**认证方式**：`Authorization: Bearer <jwt_token>`

---

### 7.2 认证接口

#### 7.2.1 管理员登录

**请求**：
```
POST /api/auth/login
```

**Body**：
```json
{
    "username": "admin",
    "password": "admin123"
}
```

**响应**（成功）：
```json
{
    "code": 0,
    "message": "login success",
    "data": {
        "token": "eyJhbGciOiJIUzI1NiIs...",
        "expires_at": "2026-08-18T10:00:00Z",
        "username": "admin",
        "role": "admin"
    }
}
```

**响应**（失败）：
```json
{
    "code": 1005,
    "message": "invalid username or password"
}
```

#### 7.2.2 刷新 Token

**请求**：
```
POST /api/auth/refresh
Authorization: Bearer <old_token>
```

**响应**：
```json
{
    "code": 0,
    "data": {
        "token": "eyJhbGciOiJIUzI1NiIs...",
        "expires_at": "2026-08-18T10:00:00Z"
    }
}
```

#### 7.2.3 修改密码

**请求**：
```
PUT /api/auth/password
Authorization: Bearer <token>
```

**Body**：
```json
{
    "old_password": "admin123",
    "new_password": "newPass456"
}
```

**响应**：
```json
{
    "code": 0,
    "message": "password updated"
}
```

---

### 7.3 路由管理接口

#### 7.3.1 获取路由列表（公开）

**请求**：
```
GET /api/routes
```

**响应**：
```json
{
    "code": 0,
    "data": [
        {
            "id": 1,
            "name": "Java-订单服务",
            "prefix": "java-order",
            "target": ":8080",
            "timeout": 5,
            "status": "active",
            "health": "healthy",
            "created_at": "2026-08-01T10:00:00Z",
            "updated_at": "2026-08-10T15:30:00Z"
        },
        {
            "id": 2,
            "name": "Go-认证服务",
            "prefix": "go-auth",
            "target": ":8081/api/v1",
            "timeout": 3,
            "status": "active",
            "health": "unhealthy",
            "created_at": "2026-08-02T11:00:00Z",
            "updated_at": "2026-08-10T16:00:00Z"
        }
    ]
}
```

#### 7.3.2 新增路由（管理员）

**请求**：
```
POST /api/routes
Authorization: Bearer <token>
```

**Body**：
```json
{
    "name": "Python-推荐服务",
    "prefix": "py-recommend",
    "target": ":5000",
    "timeout": 5,
    "status": "active"
}
```

**响应**：
```json
{
    "code": 0,
    "message": "route created",
    "data": {
        "id": 3,
        "name": "Python-推荐服务",
        "prefix": "py-recommend",
        "target": ":5000",
        "timeout": 5,
        "status": "active",
        "created_at": "2026-08-17T10:00:00Z"
    }
}
```

**错误响应**（前缀已存在）：
```json
{
    "code": 1003,
    "message": "prefix 'py-recommend' already exists"
}
```

#### 7.3.3 编辑路由（管理员）

**请求**：
```
PUT /api/routes/:prefix
Authorization: Bearer <token>
```

**Body**：
```json
{
    "name": "Python-推荐服务-新版",
    "target": ":5001",
    "timeout": 3,
    "status": "inactive"
}
```

**响应**：
```json
{
    "code": 0,
    "message": "route updated"
}
```

#### 7.3.4 删除路由（管理员）

**请求**：
```
DELETE /api/routes/:prefix
Authorization: Bearer <token>
```

**响应**：
```json
{
    "code": 0,
    "message": "route deleted"
}
```

#### 7.3.5 切换路由状态（管理员）

**请求**：
```
PATCH /api/routes/:prefix/status
Authorization: Bearer <token>
```

**Body**：
```json
{
    "status": "active"   // 或 "inactive"
}
```

**响应**：
```json
{
    "code": 0,
    "message": "status updated"
}
```

---

### 7.4 统计接口（管理员）

#### 7.4.1 统计概览

**请求**：
```
GET /api/stats/overview
Authorization: Bearer <token>
```

**响应**：
```json
{
    "code": 0,
    "data": {
        "total_pv": 1284392,
        "today_pv": 3847,
        "total_routes": 12,
        "healthy_routes": 10,
        "health_rate": 83.3
    }
}
```

#### 7.4.2 路由 PV 明细

**请求**：
```
GET /api/stats/routes?days=7
Authorization: Bearer <token>
```

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `days` | INTEGER | ❌ | 统计最近 N 天，默认 7，最大 90 |

**响应**：
```json
{
    "code": 0,
    "data": {
        "routes": [
            {
                "prefix": "java-order",
                "name": "Java-订单服务",
                "total_pv": 582391,
                "today_pv": 1204,
                "yesterday_pv": 1389,
                "trend": [1205, 1189, 1350, 1420, 1300, 1389, 1204]
            },
            {
                "prefix": "go-auth",
                "name": "Go-认证服务",
                "total_pv": 401283,
                "today_pv": 1832,
                "yesterday_pv": 1567,
                "trend": [1450, 1500, 1600, 1700, 1650, 1567, 1832]
            }
        ]
    }
}
```

#### 7.4.3 趋势图数据

**请求**：
```
GET /api/stats/trend?prefix=java-order&days=7
Authorization: Bearer <token>
```

| 参数 | 类型 | 必填 | 说明 |
| :--- | :--- | :--- | :--- |
| `prefix` | STRING | ✅ | 转发名 |
| `days` | INTEGER | ❌ | 统计最近 N 天，默认 7 |

**响应**：
```json
{
    "code": 0,
    "data": {
        "labels": ["08-10", "08-11", "08-12", "08-13", "08-14", "08-15", "08-16"],
        "values": [1205, 1189, 1350, 1420, 1300, 1389, 1204],
        "total": 9067,
        "avg": 1295.3
    }
}
```

#### 7.4.4 清理日志（管理员）

**请求**：
```
POST /api/stats/cleanup
Authorization: Bearer <token>
```

**Body**：
```json
{
    "retain_days": 90   // 保留最近 N 天，默认 90
}
```

**响应**：
```json
{
    "code": 0,
    "message": "cleaned 15342 records",
    "data": {
        "deleted_count": 15342,
        "remaining_count": 871234
    }
}
```

---

## 8. 界面原型

### 8.1 访客首页

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🔗 GatewayHub 服务看板                                          [登录]    │
│  ─────────────────────────────────────────────────────────────────────────── │
│  📊 服务总数: 12  │  🟢 健康: 10  │  🔴 不健康: 2  │  ⏱ 最后更新: 10s前  │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ 服务名称      │ 转发名         │ 后端地址          │ 状态            │ │
│  │ ───────────── │ ────────────── │ ───────────────── │ ─────────────── │ │
│  │ Java-订单服务  │ /java-order   │ :8080            │  🟢 健康        │ │
│  │ Go-认证服务    │ /go-auth      │ :8081/api/v1     │  🟢 健康        │ │
│  │ Node-推送服务  │ /node-push    │ :3000            │  🔴 不健康      │ │
│  │ Python-推荐    │ /py-recommend │ :5000            │  🟢 健康        │ │
│  │ PHP-后台管理   │ /php-admin    │ :8082/admin      │  ⚪ 未知        │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│  💡 提示：访问格式为 http://gateway:8088/{转发名}/{自有路径}               │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 8.2 管理员登录弹窗

```
┌──────────────────────────────────┐
│  🔐 管理员登录          [✕]     │
│  ┌──────────────────────────────┐│
│  │ 用户名  [admin          ]   ││
│  │ 密码    [········       ]   ││
│  │ ☑ 记住我 (7天有效)          ││
│  │        [  登录  ]            ││
│  │ 默认管理员: admin/admin123   ││
│  └──────────────────────────────┘│
└──────────────────────────────────┘
```

### 8.3 管理员后台 - 路由管理

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  🔗 GatewayHub                                    admin ▼                 │
│  ─────────────────────────────────────────────────────────────────────────── │
│  [📋 路由管理]  [📊 统计面板]                                              │
│  ─────────────────────────────────────────────────────────────────────────── │
│  [+ 新增路由]                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ 服务名称    │ 转发名     │ 后端地址      │ 超时 │ 状态 │ 健康  │ 操作 │ │
│  │ ─────────── │ ────────── │ ───────────── │ ──── │ ──── │ ────  │ ──── │ │
│  │ Java-订单   │ /java-order│ :8080         │ 5s   │ ●启用│ 🟢   │ [✏️][🗑]│ │
│  │ Go-认证     │ /go-auth   │ :8081/api/v1  │ 3s   │ ●启用│ 🟢   │ [✏️][🗑]│ │
│  │ Node-推送   │ /node-push │ :3000         │ 5s   │ ○停用│ 🔴   │ [✏️][🗑]│ │
│  │ Python-推荐 │ /py-rec    │ :5000         │ 5s   │ ●启用│ 🟢   │ [✏️][🗑]│ │
│  └────────────────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 8.4 新增路由弹窗

```
┌────────────────────────────────────────────────┐
│  ➕ 新增路由                          [✕]     │
│  ┌────────────────────────────────────────────┐│
│  │ 服务名称 * [________________________]     ││
│  │ 提示: 如 "Java-订单服务"                   ││
│  │                                            ││
│  │ 转发名 *   [________________________]     ││
│  │ 提示: 如 "java-order"，仅小写/数字/-      ││
│  │                                            ││
│  │ 后端地址 * [________________________]     ││
│  │ 提示: :8080 或 :8080/api/v1 或完整 URL    ││
│  │                                            ││
│  │ 超时时间   [5] 秒                         ││
│  │                                            ││
│  │ 状态       [● 启用] [○ 停用]             ││
│  │                                            ││
│  │         [取消]     [确认添加]             ││
│  └────────────────────────────────────────────┘│
└────────────────────────────────────────────────┘
```

### 8.5 统计面板

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  📋 路由管理  │  📊 统计面板                      admin ▼                 │
│  ─────────────────────────────────────────────────────────────────────────── │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ ┌───────────────┐ │
│  │ 📊 总 PV      │ │ 📈 今日 PV    │ │ 📋 路由总数   │ │ ❤️ 健康率    │ │
│  │ 1,284,392     │ │ 3,847         │ │ 12            │ │ 83.3%        │ │
│  └───────────────┘ └───────────────┘ └───────────────┘ └───────────────┘ │
│  ─────────────────────────────────────────────────────────────────────────── │
│  📈 近 7 天访问趋势                    [近7天 ▼]  [近30天]                │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │          ┌────┐                                                       │ │
│  │       ┌──┘    └──┐                                                    │ │
│  │    ┌──┘          └──┐                                                 │ │
│  │ ┌──┘                └──┐                                              │ │
│  │ ┘                      └──┐                                           │ │
│  │  08-10  08-11  08-12  08-13  08-14  08-15  08-16                    │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│  ─────────────────────────────────────────────────────────────────────────── │
│  📋 路由 PV 明细                                                          │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │ # │ 转发名      │ 服务名      │ 总 PV   │ 今日 PV │ 昨日 PV │ 趋势    │ │
│  │ ─ │ ─────────── │ ─────────── │ ─────── │ ─────── │ ─────── │ ────── │ │
│  │ 1 │ /java-order │ Java-订单   │ 582,391 │ 1,204   │ 1,389   │ ╱╲╱╲╱ │ │
│  │ 2 │ /go-auth    │ Go-认证     │ 401,283 │ 1,832   │ 1,567   │ ╱╱╲╲╱ │ │
│  │ 3 │ /py-rec     │ Python-推荐 │ 300,718 │ 811     │ 945     │ ╲╱╲╱╲ │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│  共 12 条记录，每页 10 条    [< 1 2 3 >]                                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 9. 技术方案

### 9.1 技术栈

| 层级 | 技术选型 | 版本 | 说明 |
| :--- | :--- | :--- | :--- |
| **后端语言** | Go | 1.21+ | 标准库原生支持反向代理，性能优异 |
| **Web 框架** | Gin | v1.9+ | 轻量高性能，中间件生态丰富 |
| **ORM** | GORM | v1.25+ | 支持 SQLite/MySQL/PostgreSQL 切换 |
| **数据库** | SQLite / MySQL | 3.x / 8.0 | SQLite 默认，MySQL 可选 |
| **反向代理** | `net/http/httputil.ReverseProxy` | 标准库 | Go 原生实现，稳定可靠 |
| **JWT** | `golang-jwt/jwt` | v5 | 无状态认证 |
| **日志** | `uber-go/zap` | v1.26+ | 高性能结构化日志 |
| **配置管理** | `spf13/viper` | v1.18+ | 支持 YAML/ENV 配置 |
| **前端框架** | Vue 3 | v3.3+ | Composition API，轻量响应式 |
| **前端构建** | Vite | v4.5+ | 快速构建，热更新 |
| **UI 组件** | Element Plus | v2.5+ | 完整组件库，快速搭建 |
| **图表** | ECharts | v5.4+ | 趋势图、饼图等 |
| **HTTP 客户端** | Axios | v1.6+ | 前端 API 请求 |

### 9.2 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              客户端（浏览器 / API 调用）                    │
│                                    │                                       │
│                                    ▼                                       │
│  ┌───────────────────────────────────────────────────────────────────────┐ │
│  │                           GatewayHub 网关                            │ │
│  │  ┌─────────────────────────────────────────────────────────────────┐ │ │
│  │  │                      HTTP 路由分发器                           │ │ │
│  │  │  1. 解析路径 → 提取转发名 → 匹配路由表                         │ │ │
│  │  │  2. 路径重写（拼接后端根路径）                                  │ │ │
│  │  │  3. 反向代理转发 → 异步记录日志                                │ │ │
│  │  └─────────────────────────────────────────────────────────────────┘ │ │
│  │                           │                                         │ │
│  │          ┌────────────────┼────────────────┐                       │ │
│  │          ▼                ▼                ▼                       │ │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐              │ │
│  │  │  健康检查器   │ │  路由管理器   │ │  统计服务    │              │ │
│  │  │  每30s探测   │ │  热加载配置   │ │  异步写入   │              │ │
│  │  └──────────────┘ └──────────────┘ └──────────────┘              │ │
│  └───────────────────────────────────────────────────────────────────────┘ │
│                              │ │ │                                       │
│              ┌───────────────┘ │ └───────────────┐                      │
│              ▼                 ▼                 ▼                       │
│  ┌────────────────┐ ┌────────────────┐ ┌────────────────┐              │
│  │   SQLite/MySQL  │ │   内存路由表    │ │   日志缓冲     │              │
│  │   持久化存储    │ │   (sync.Map)   │ │   (Channel)    │              │
│  └────────────────┘ └────────────────┘ └────────────────┘              │
│                                 │ │ │                                   │
│           ┌─────────────────────┘ │ └─────────────────────┐            │
│           ▼                       ▼                       ▼            │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐        │
│  │ Java 服务    │      │ Go 服务      │      │ Python 服务  │        │
│  │ :8080        │      │ :8081/api/v1 │      │ :5000        │        │
│  └──────────────┘      └──────────────┘      └──────────────┘        │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 9.3 数据流向

#### 9.3.1 请求转发流程

```
1. 客户端 → 网关: GET /java-order/user/info
2. 网关解析: prefix = "java-order", path = "/user/info"
3. 内存查找: routes["java-order"] → target=":8080", root=""
4. 路径拼接: /user/info (无根路径，直接转发)
5. 转发后端: GET http://127.0.0.1:8080/user/info
6. 异步记录: access_logs 表插入一条记录
7. 响应返回: 后端响应 → 网关 → 客户端
```

#### 9.3.2 配置热加载流程

```
1. 管理员提交新增路由表单
2. 后端写入 routes 表
3. 后端更新内存路由表 (sync.RWMutex.Lock)
4. 内存路由表添加新条目 ReverseProxy 实例
5. 下一个请求立即命中新路由（无需重启）
```

### 9.4 性能优化方案

| 优化点 | 方案 | 预期收益 |
| :--- | :--- | :--- |
| **路由查找** | 使用 `sync.Map` 存储路由表，O(1) 查找 | 查找耗时 < 100ns |
| **连接复用** | Go 标准库 `Transport` 默认开启连接池 | 减少 TCP 握手开销 |
| **日志写入** | 异步批量写入（Channel + Batch Insert） | 转发延迟不增加 |
| **健康检查** | 独立 Goroutine，不阻塞主流程 | 不影响转发性能 |
| **静态资源** | 前端资源嵌入二进制，减少文件 IO | 启动速度快 |
| **GORM 配置** | 禁用默认事务，使用 `PrepareStmt` | 查询性能提升 30% |

### 9.5 安全方案

| 安全点 | 方案 |
| :--- | :--- |
| **密码存储** | bcrypt (cost=10) 哈希存储 |
| **Token 传递** | HTTP Header `Authorization: Bearer <token>` |
| **Token 刷新** | 提供 `/auth/refresh` 接口，无感续期 |
| **SQL 注入** | GORM 参数化查询 |
| **XSS 防护** | Vue 自动转义，后端不输出 HTML |
| **CORS** | 配置允许的来源（生产环境限制域名） |

---

## 10. 部署方案

### 10.1 部署形态

**单一二进制部署**（推荐）：
- 后端 Go 二进制文件（包含前端静态资源）
- 依赖：SQLite 数据库文件（自动生成）
- 启动命令：`./gatewayhub -config=config.yaml`

### 10.2 配置文件（config.yaml）

```yaml
server:
  host: "0.0.0.0"
  port: 8088
  read_timeout: 30s
  write_timeout: 30s

database:
  driver: "sqlite"           # sqlite / mysql / postgres
  dsn: "gateway.db"          # SQLite: 文件路径
  # dsn: "user:pass@tcp(127.0.0.1:3306)/gateway?charset=utf8mb4"  # MySQL

jwt:
  secret: "your-secret-key-change-me-in-production"
  expires: 24h

health_check:
  interval: 30s
  timeout: 2s
  fail_threshold: 3
  recover_threshold: 2
  health_endpoint: "/health"

stats:
  retain_days: 90
  batch_size: 100
  batch_interval: 1s
  sample_rate: 1.0          # 1.0 = 100% 记录

log:
  level: "info"              # debug / info / warn / error
  output: "stdout"           # stdout / file
  file_path: "/var/log/gatewayhub.log"
```

### 10.3 启动命令

```bash
# 开发环境
go run main.go -config=config.dev.yaml

# 生产环境
./gatewayhub -config=/etc/gatewayhub/config.yaml

# 后台运行（systemd）
systemctl start gatewayhub
systemctl enable gatewayhub
```

### 10.4 Systemd 服务配置

```ini
[Unit]
Description=GatewayHub Aggregation Gateway
After=network.target

[Service]
Type=simple
User=gateway
Group=gateway
WorkingDirectory=/opt/gatewayhub
ExecStart=/opt/gatewayhub/gatewayhub -config=/etc/gatewayhub/config.yaml
Restart=always
RestartSec=10
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
```

### 10.5 环境要求

| 环境 | 最低配置 | 推荐配置 |
| :--- | :--- | :--- |
| **操作系统** | Linux (kernel 4.x+) / macOS / Windows | Linux Ubuntu 20.04+ |
| **CPU** | 2 核 | 4 核+ |
| **内存** | 512 MB | 2 GB+ |
| **磁盘** | 1 GB (不含日志) | 10 GB+ (含日志) |
| **数据库** | SQLite 3.x (内置) | MySQL 8.0 / PostgreSQL 14+ |

---

## 11. 项目排期

### 11.1 总体时间线

| 阶段 | 时间 | 交付物 |
| :--- | :--- | :--- |
| **需求评审** | 第 1 周 | 需求文档定稿 |
| **技术设计** | 第 2 周 | 技术方案设计、数据库设计、接口设计 |
| **后端开发** | 第 3-4 周 | 转发引擎 + API + 统计模块 |
| **前端开发** | 第 4-5 周 | Vue 管理控制台 |
| **联调测试** | 第 6 周 | 前后端联调、功能测试、性能测试 |
| **部署上线** | 第 7 周 | 生产环境部署、灰度验证 |
| **运维交接** | 第 8 周 | 文档归档、运维培训 |

### 11.2 任务分解

| 模块 | 子任务 | 工时（人天） | 负责人 |
| :--- | :--- | :--- | :--- |
| **后端** | 路由引擎 + 反向代理 | 3 | 后端开发 |
| | 路由 CRUD API | 2 | 后端开发 |
| | 健康检查 | 2 | 后端开发 |
| | 统计模块（异步写入 + 查询） | 4 | 后端开发 |
| | 认证模块（JWT + 密码） | 2 | 后端开发 |
| | 数据库设计 + GORM 配置 | 2 | 后端开发 |
| | 配置热加载 | 2 | 后端开发 |
| **前端** | 访客页面（服务列表） | 2 | 前端开发 |
| | 管理员登录 | 1 | 前端开发 |
| | 路由管理页面（CRUD） | 4 | 前端开发 |
| | 统计面板（卡片 + 趋势图） | 4 | 前端开发 |
| | 路由 PV 明细 + 详情 | 3 | 前端开发 |
| **测试** | 功能测试用例编写 | 2 | 测试 |
| | 功能测试执行 | 3 | 测试 |
| | 性能测试（压测） | 2 | 测试 |
| **运维** | 部署脚本 + 文档 | 2 | 运维 |

**总工时**：约 40 人天（2 名后端 + 1 名前端 + 1 名测试）

---

## 12. 附录

### 12.1 项目启动快速指南

**1. 克隆代码**：
```bash
git clone https://github.com/your-org/gatewayhub.git
cd gatewayhub
```

**2. 后端启动**：
```bash
go mod download
go run main.go
```

**3. 前端开发**：
```bash
cd web
npm install
npm run dev
```

**4. 访问地址**：
- 网关服务：`http://localhost:8088`
- 前端开发：`http://localhost:5173`
- 默认管理员：`admin` / `admin123`

### 12.2 常见问题 FAQ

| 问题 | 解答 |
| :--- | :--- |
| **如何修改默认端口？** | 修改 `config.yaml` 中的 `server.port` 字段 |
| **如何重置管理员密码？** | 直接修改数据库 `users` 表中 admin 的 `password` 字段，使用 bcrypt 生成新密码 |
| **支持 HTTPS 吗？** | V1.0 不支持，通过前置 Nginx 或云 LB 卸载 SSL |
| **日志量太大怎么办？** | 调整 `stats.sample_rate` 参数（如 0.1 表示 10% 采样），或缩短 `stats.retain_days` |
| **如何迁移到 MySQL？** | 修改 `database.driver` 为 `mysql`，更新 `database.dsn` 连接串，重启即可 |
| **支持多节点部署吗？** | 支持，多节点共享同一个数据库，各自维护内存路由表 |

### 12.3 参考文档

| 文档 | 链接 |
| :--- | :--- |
| Go 官方文档 | https://go.dev/doc/ |
| Gin 框架 | https://gin-gonic.com/docs/ |
| GORM 文档 | https://gorm.io/docs/ |
| Vue 3 文档 | https://vuejs.org/guide/ |
| Element Plus | https://element-plus.org/ |
| ECharts | https://echarts.apache.org/ |

---

> **文档结束**

---

这份完整版需求文档涵盖了从项目背景到具体实现的所有细节。如果你确认无误，我下一步可以直接提供：

1. **完整的 Go 后端代码**（含所有模块）
2. **完整的 Vue 3 前端代码**（含所有页面）
3. **Docker 部署配置**
4. **测试用例文档**

需要我先提供哪一部分？😊